Талесников Святослав ИКБО-71-24 Вариант 24
Задание 3 Этап 2

Реализовал транслятор из промежуточного в машинное представление 
Записываем результат ассемблирования в двоичный выходной файл 
Выводим на экран размер двоичного файла в байтах 
В режиме тестирования вывел результат в байтовом формате, как в спецификации 
Создал тестовую программу, соответствующую всем тестовым последовательностям из спецификации 
Поддерживаемые команды:

 `load rN = K` — загрузка константы `K` в регистр `rN`
 `read rN = [rM + offset]` — чтение значения из памяти по адресу `rM + offset` в регистр `rN`
 `write [rM + offset] = rN` — запись значения регистра `rN` в память по адресу `rM + offset`
 `sgn [rSrc] -> [rDst]` — применение унарной операции `sgn()` к значению в памяти по адресу `rSrc`, результат записывается по адресу `rDst`
